import React from 'react';
import {HeaderAndFooter} from "../components";

const Root:React.FC = () => {
    return (
        <>
            <HeaderAndFooter>
                Main
            </HeaderAndFooter>
        </>
    );
};

export default Root;
